.map-container
  #map.map
  .meta
    .trail
      %h2.name
        = @trail.display_name
        %small
          = @trail.status
          \-
          %span{title: @trail.updated_at}
            = time_ago_in_words @trail.updated_at
            ago
    .loop
      %h2.name
:javascript
  mtb.trail = #{@trail.geojson.to_json};
